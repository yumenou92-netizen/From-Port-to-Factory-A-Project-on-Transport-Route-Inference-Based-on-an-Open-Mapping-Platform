# PLAN.md

Updated: 2026-07-31

本文件只记录当前真实工程计划。完成状态以当前代码、测试和 `docs/PROJECT_STATE.md` 为准；领导汇报、每日计划和历史报告不能替代工程事实。

## 开发主线

```text
本地真实数据
-> 数据审计与类型化加载
-> 节点、坐标、订单和单位校验
-> 运价选择、费用和时间 Provider
-> CustomerProfile 路线分支
-> 两个业务运输段的可行边生成
-> TransportEdge（运输方式 / 业务段 / 图边角色分离）
-> MultiDiGraph
-> cost / time 独立搜索
-> RouteResult 可解释输出
-> 稳定的路线规划请求/响应应用契约
-> 全流程 Demo / Web 展示适配器
-> 真实业务数据替换占位
-> 展示收束与后续生产化评估
```

## 里程碑状态

| 阶段 | 里程碑 | 状态 | 验收依据或剩余边界 |
|---|---|---|---|
| 0 | 长期项目记忆和交接体系 | 已完成 | `AGENTS.md`、`PLAN.md`、`PROJECT_STATE.md`、`ARCHITECTURE.md`、`DECISIONS.md`、`CHANGELOG.md` 已建立 |
| 1 | JSON/JSONL 数据审计和脱敏输出 | 已完成（第一版） | 支持质量统计；详细输出只保留在本地 `output/` |
| 2 | 真实数据类型化加载 | 已完成（第一版） | 通过 `DATA_DIR` 加载运价、节点和 AdditionalFee，异常显式返回 |
| 3 | NodeRegistry 和坐标解析 | 已完成（基础版） | 本地优先、腾讯兜底；缓存、抽检和正式回写未完成 |
| 4 | RouteRequest、单位和包装校验 | 已完成（第一版） | `吨/箱/柜` 与价格单位精确匹配，不自动互换 |
| 5 | FreightRate、CostRuleEngine 和最新运价 | 已完成（第一版） | 熟悉路线、散船模式、陌生散粮和集装箱汽运规则均有测试与追溯 |
| 6 | ShippingTimeProvider | 已完成（第一版） | 人工 Provider 可用，JSON/数据库/API Provider 仅保留接口 |
| 7 | CustomerProfile 和路线分支 | 已完成（第一版） | 自有码头与候选中转港分支已建模；真实客户字段未接入 |
| 8 | TransportEdge 和真实候选桥接 | 已完成（保守第一版） | 节点、费用、时间和来源齐全才形成可搜索边 |
| 9 | `nx.MultiDiGraph` 正式图 | 已完成（第一版） | 保留平行边，以 edge ID 为 key，排除项可追溯 |
| 10 | cost/time 双目标搜索 | 已完成（第一版） | NetworkX Dijkstra 独立搜索成本和时间，不合成权重 |
| 11 | RouteResult 和解释输出 | 已完成（第一版） | 返回节点、edge key、分段费用/时间/来源和显式异常状态 |
| 12 | 代码分层和旧原型剥离 | 已完成（第一版） | `data/domain/geo/routing/demos/dev` 分层，旧 CSV/DiGraph 主链已移除 |
| 13 | 本地运行环境表和开发者 smoke | 已完成（第一版） | 本地 CSV 注入环境变量；配置模板可提交，真实配置被忽略 |
| 14 | 腾讯地点多候选策略 | 已完成（原型口径） | 多候选默认 top1，不阻塞；保留 top 5 和置信等级供抽检 |
| 15 | 真实数据候选进入正式搜索链 | 已完成（保守第一版） | 首批节点补录后，本地演示人工时效下 152 条边可入图；该时效不代表真实业务值 |
| 16 | 第一版全流程领导 Demo | 已完成（基础版） | A/B 输入到费用最低、时间最短路线已贯通；占位范围显式受限 |
| 17 | PyCharm 真实 A/B 预演与展示收束 | 已完成 | 用户 PyCharm 最新版 `leader full-flow` 已成功运行；中文口径、候选说明、费用组成和边界声明符合阶段 17 验收口径 |
| 18 | 用正式业务数据替换演示占位 | 进行中（W1/B1/W2/W4 已完成；W3/W5 已进入可测算闭环；第二业务段首个驳船中转实例与本地展示页完成第一版） | 散船真实费率和完整航运段时效已进入 full-flow；W5 精确费率优先并允许同业务区域最近正式费率作为可追溯 `regional_proxy`；福建闽江 15 小时、50 元/吨规则已数据化；南港至客户已验证直接汽运和“驳船至中转港后汽运”实例，但长期结构是可包含任意数量中转边的多式联运子路径，不限于这两种模板；人工指定南港已可用；本地腾讯地图展示页已复用同一 full-flow 边界。剩余核心边界是通用第二段候选生成、正式港口能力、更多运输方式、部分海港干线资格及权威航道几何 |
| 19 | 生产化范围评估 | 未开始 | 数据库/API/前端、缓存、权限、监控和性能均未立项，不得视为已有能力 |

## 当前里程碑

阶段 17 的核心工程、展示口径和用户 PyCharm 最新实跑验收已经完成。经用户明确授权，阶段 18 已开始；当前完成了不依赖未确认费率口径的 W1 数据契约，并于 2026-07-23 完成 B1 业务口径签认：

1. `NodeProfile` 明确基础设施类型、标准所在地、时效分区、作业能力和来源；
2. `TransportStage` 只区分 `north_to_south` 和 `south_to_customer` 两个业务运输段；运输方式继续由 `transport_mode` 表示，`TransportEdgeRole` 独立表示 `trunk`、`transfer`、`delivery`；
3. `TimeScope` 明确完整运输段和道路驾驶；当前散船、驳船的业务“航行时效”统一表示完整航运段总时间，缺失范围保持未知，不静默补值；
4. `CostComponent`/`CostComposition` 保留费用分项并校验合计；
5. `ManualReviewOutcome` 为后续加载器、Provider 和 full-flow 提供统一人工复核追溯；
6. 散船报价单位、费用公式、适用北港、南港作业费首版口径和税务边界已确认；
7. W2/W4 最小闭环已接入 `leader_full_flow`：读取散船运价表最新行，保留同一区域多船型列，按订单吨数向上选择可承载船型，并按已确认时效分区将完整航运段天数换算为小时；
8. 自动模式下，无法匹配真实散船目的组或完整航运段时效的候选南港不形成干线边，保留运行提示，不再使用北—南港干线占位。
9. 内河航运范围包括福建闽江以及珠三角—西江沿线已确认区域；港口连通性由业务运输段、运输方式、包装/品种和港口作业能力决定；内河驳船时效可独立接入，但航费和能力未齐前不生成真实边；北港至南港散船费率/时效继续通过可追溯区域映射匹配；当前客户工厂统一按无自有码头处理。
10. 300 公里强制汽运预筛已取消，不写入规则、不进入算法。
11. 已新增港口能力表、区域映射表、内河航运航费/航时表的代码接口，并实现福建闽江/珠三角最小 `demo_placeholder` 驳船 Provider；暂未自动接入 `leader_full_flow` 默认主链。
12. 2026-07-24 已将港口能力表和区域映射表推进为可加载 CSV 接口并纳入只读数据夯实审计；缺失表只输出接口提示，不猜测港口能力或区域映射。
13. 2026-07-24 已新增南港“码头作业费”元/吨汇总 Provider 接口、CSV 读取和显式 `demo_placeholder` Provider；真实作业费缺失、重复、单位不匹配或订单不匹配时返回 `manual_review`，不计入费用且不解释为 0。
14. 2026-07-24 已新增 `docs/data_templates/` W3/W5 样表和说明文档，并通过专项测试保证样表字段能被当前 Loader 读取。
15. 2026-07-24 已将 W5 作业费 Provider 以可选正式输入接入 `leader_full_flow`：未接入作业费表时不改变当前 Demo；一旦接入正式表，散船干线边必须匹配作业费才可入图，已匹配作业费作为独立 `CostComponent` 进入总费用组成，未匹配候选输出运行提示并排除，避免按 0 参与费用最优。
16. 2026-07-24 已在订单和 W5 作业费中补充 `tradeType`/`trade_type` 维度；当前 Demo 默认 `内贸`，但数据结构允许维护 `外贸`，同一码头可按品种、贸易类型和包装方式维护不同费率。
17. 2026-07-24 已新增 `部分码头标签.json` 只读审计入口：将 `serviceFees.入库` 作为候选“码头作业费”单位费率，保留贸易类型和品种范围；审计结果不自动写入正式 CSV，别名治理顺序为“名称字典 -> 腾讯地图 API -> 人工确认”。
18. 2026-07-24 已完成该 JSON 的名称字典优先阶段：只允许港口后缀归一化后完全一致且唯一指向一个已注册节点的审计候选绑定，不注册新别名；4 个去重名称、9 条展开费率规则已由名称字典吸收，剩余 23 个去重名称已生成 Tencent 候选查询清单。Tencent 结果仍须人工确认，确认前不写正式 W3/W5 CSV。
19. 2026-07-24 已处理首轮人工确认和明确候选回写：新增 10 个标准坐标并写入 14 组明确别名关系；`肇庆福加德码头` 绑定客户公司节点并归类为客户自有码头；`汇东/百达/红东` 明确忽略。刷新审计后直接节点命中由 13 条升至 60 条，未命中由 56 条降至 9 条、去重名称由 23 个降至 7 个；当前真正待地点确认的是 `炮台港/金港`，另有 2 个客户工厂标签的零费率业务含义待确认。由于领导数据仅为“部分码头标签”，尚未用正式文件名启用 W5 表，避免严格缺失费规则提前排除未覆盖南港。
20. 2026-07-24 已完成 W5 地域代理与路线标签契约：作业费按“精确费率 → 唯一已确认 `operation_fee_region_code` 下的唯一参考码头费率 → 人工复核”执行；映射只接受标准 `node_id` 的人工维护记录，不使用城市关键词、名称或坐标自动推断。`regional_proxy` 保留参考码头、映射依据、规则编号/版本并声明不是目标码头精确真实费率。港口能力接口改为三态，未知能力留空且不启用正式过滤；节点/客户画像补充支持包装、品种、运输方式、确认状态、维护日期和客户自有码头关系契约。
21. 2026-07-27 用户批准当前 W5 正数候选和两条客户自有码头 0 元记录进入测算，其他未绑定正数记录忽略。本地正式表已生成 69 条 `chargeable` 费率和 2 条 `not_applicable` 规则；后者明确表示无需码头作业费，不是缺失补零，也不生成虚假零值费用分项。
22. 2026-07-28 领导展示模式新增 W5 第三优先级：精确费率和人工维护区域代理均缺失时，在可追溯的同一业务区域内选择地理距离最近、且适用当前订单的正式码头费率；结果标记 `regional_proxy` 并保留参考码头、距离、区域和映射依据。
23. 2026-07-28 新增安全的散船航时最近区域代理接口：只有港口能力表唯一确认目标港可接北港散船后，才允许借用同区域最近参考港的完整航运段时效；该接口不代理运价目的组、不证明连通性，当前能力缺失港仍不自动入图。
24. 2026-07-28 福建闽江驳船正式费率/时效接口已落地：往返完整运输段均为 15 小时，散粮为 50 元/吨；正式 Provider 只有在区域、费率、时效和端点能力同时满足时才生成驳船边，能力仍为占位时在输出中明确标为 `demo_placeholder`。该规则只是一条第二业务段运输边的数据实例。
25. 2026-07-28 `leader_full_flow` 已支持南港至客户业务段中的汽运边和驳船中转边共同参与搜索，并支持 `--south-port` 人工指定南港；这不是对第二业务段路线类型的穷举。
26. 2026-07-28 已建立本地展示页第一版：复用 full-flow 输入和计算链，提供运输条件、订单信息、双目标结果、费用组成、运行警告与腾讯地图节点示意；默认仅监听回环地址，地图连线明确不是道路或航道真实几何。
27. 2026-07-28 用户确认当前腾讯 Key 同时具备 JavaScript API 和 WebService API 权限；浏览器实测已完成共享 Key 的地图底图加载、后端路线测算、节点/示意线绘制和无控制台错误验证。修复了结果出现后空状态仍叠加显示的问题。
28. 2026-07-28 本地展示页已升级为分段几何：散船干线使用中国近海控制点生成的测地虚线示意，汽运保存并解码同一次腾讯驾车响应的 `routes[0].polyline`；几何通过 edge ID 侧表传递，不写入 `TransportEdge`，不改变费用、时效或 Dijkstra。腾讯道路折线缺失时明确标为不可用，不以节点直线冒充。默认实测路线的汽运段返回 531 个道路点；新的港口组合仍须目视抽检近海控制点，必要时使用人工控制点钩子微调。
29. 2026-07-28 新增订单参数/港口入图只读审计：自动从当前真实运价提取玉米、小麦、大豆，覆盖散粮/吨、集装箱/箱、集装箱/柜、内外贸及全部散船船型承载边界和边界后一吨；逐场景检查候选来源、散船干线和码头作业费，输出 ignored 的逐港状态与人工复核 CSV。审计不调用 Tencent，并把“离线前置已通过”与“道路边已实际入图”明确分开。
30. 2026-07-28 按用户确认收紧南港名称角色：名称含“站”或为“地名+方位”的节点按铁路站排除；名称含“库/仓/公司”的节点按客户工厂/仓库排除；客户自有码头必须维护为独立港口/码头节点。自动候选不足时只允许回退未分类节点，不再回退铁路站或客户节点；显式 `--south-port` 同样拒绝这两类节点。
31. 2026-07-29 完成首批港口水域角色确认：东莞深粮、平和县储备粮固定为客户工厂；清远清新码头、苏湾港、贵港白沙码头、韶关北江国际港（白土码头）固定为内河码头，不得作为北港散船干线终点；军航码头确认为福建闽江海河双用港并显式映射到马尾费率目的组和福建航时区，洋浦港确认为海港并显式映射到马村/海口费率目的组和海南航时区。映射来源为业务确认，不是名称或坐标自动猜测。
32. 2026-07-29 接入 `码头信息维护0729版.xlsx` 作为最新本地节点主数据覆盖层：保留既有稳定节点 ID，使用工作簿全称/简称扩充别名并优先采用其坐标，新旧坐标差异继续进入冲突审计；节点注册不等同于运输能力准入。
33. 2026-07-29 闽江航线进一步收束为仅维护 `南平港↔军航码头`：南平港支持散粮、集装箱、铁路和驳船，品种为玉米/小麦，但作为纯内河港不得成为北港散船干线终点；往返散粮驳船确认使用 50 元/吨、完整运输段 15 小时。福州马尾港退出当前闽江构边范围。
34. 2026-07-29 北港散船的南港终点改为必须具有明确 `sea_port` 或 `sea_inland_dual_use` 标识；纯内河港和未确认水域角色港口均不进入自动候选，也不能通过 `--south-port` 绕过。此前第 30 项的“未分类始发端兼容回退”在本运输阶段停止使用。
35. 2026-07-29 已将闽江第二业务段中转实例接入 `leader_full_flow`：对军航码头候选，同时保留军航码头直达客户汽运，以及 `军航码头→南平港` 驳船加 `南平港→客户` 汽运两种当前可用方案；客户工厂不再直接作为驳船端点。该“三条图边”实例不表示项目有三个业务运输段，也不限制未来其他中转节点或运输方式。
36. 2026-07-29 已生成 77 个缺海港标识节点的一次性两列确认表；此前已确认忽略的百达码头、红东码头不再列为待确认。阶段展示前改为“人工确认数据 + 工程开发”双轨推进，详细 SOP 见 `docs/plans/2026-07-29-002-stage-demo-dual-track-plan.md`。
37. 2026-07-29 已完成长期架构口径收束：正式契约把业务运输段、运输方式和图边角色拆开；full-flow 不再硬编码南平港，而是从当前驳船 Provider 的能力记录枚举并解析中转端点。南平港继续作为闽江已确认数据实例，不再作为通用架构常量。
38. 2026-07-29 已确认领导需求的权责边界：现有 `leader_full_flow.py` 和 Web 输入/输出形式属于必须保留的产品 IO 验收口径；模型运算、推断算法、候选生成、图结构和模块设计由工程侧在已确认业务规则下独立实现。后续应把 full-flow 中仍与展示混合的编排逐步下沉为界面无关的路线规划应用服务，CLI Demo 和 Web 只作为同一请求/响应契约的适配器；该收束不得改变现有业务公式或为重构而扩张当前里程碑范围。
39. 2026-07-30 已完成应用层边界第一步：新增 `RoutePlanningRequest`、`RoutePlanningResponse` 和 `RoutePlanningService`，CLI 与 Web 已改用同一应用请求/响应入口，原 `FullFlowDemoResult` 名称保留兼容；费用、航时、候选港、构图和双目标搜索行为未改变。full-flow 具体编排尚在 `leader_full_flow.py`，下一步只做渐进迁移，不进行大规模重构。
40. 2026-07-30 已按最新业务确认调整北港散船南港准入：标准港口/码头节点只要在正式运价源中存在适用当前品种的散粮始发运价，即可作为南港身份依据，驳船和汽运记录均可提供该证据；已确认纯内河港、铁路站、客户设施及显式散粮能力排除仍优先阻断。仅出现集装箱始发运价的港口不作为南港，只标记为“潜在中转港待确认”；尚未自动赋予中转港角色。
41. 2026-07-30 已为 W3 港口能力契约加入三态 `is_transfer_port`/`是否中转港` 字段、CSV 别名读取、数据基础审计列和模板说明。第二业务段的驳船 `transfer` 候选终点必须明确标记为中转港；空值保持未知并阻断该中转端点。南平港和当前海河衔接实例按既有业务确认标记，10 个仅集装箱始发港口仍不自动赋值。
42. 2026-07-30 已完成 E2 候选决策可解释性第一版：应用响应新增结构化 `SouthPortCandidateDecision`，记录每个本次订单相关始发节点在身份筛选、候选排序、散船干线、码头作业费、南港后运输和最终入图阶段的状态、原因与证据。CLI 与 Web 使用同一结果展示纳入、排除和超出候选上限的原因，不改变费用、航时、构图或双目标搜索。
43. 2026-07-30 已完成应用层渐进迁移第二步：南港自动/人工选择、散粮始发证据判断和候选排序已从 `leader_full_flow.py` 下沉到 `src/application/south_port_selection.py`。模块返回统一 `SouthPortSelection` 和候选决策；Demo 只转换应用异常、继续组织 Provider/构图并展示结果。应用层直接测试与 full-flow/Web 回归通过，未改变领导 IO。
44. 2026-07-30 已完成本轮收束验证：统一开发 smoke 为 `434 passed in 2.61s`，真实数据 smoke 仍为 492 条运价、367 个标准节点、312 条已计费且图端点完整候选、0 条缺节点 ID。只读 W3/W5 审计同步刷新；港口能力正式表仍为 0 条，但空表审计输出已保留 `is_transfer_port` 等完整字段，便于后续数据平台或人工维护，不把接口就绪误述为正式数据已上线。
45. 2026-07-30 已按 Web 展示复核修正散船示意几何：黄海—东海—台湾海峡—南海控制点整体向大陆沿海收近；广西海港方向固定经琼州海峡进入北部湾，不再绕行海南岛南部。该变化只影响展示几何，不参与费用、时效、候选、构图或 Dijkstra；相关 Web 几何/序列化专项测试为 `12 passed in 0.42s`。
46. 2026-07-31 已接入 `节点信息维护0731.xlsx`：最新工作表作为只读节点主数据适配输入，校验 17 列表头、过滤别名占位值、优先采用新坐标，并对同名记录做保守合并；不同名称的港口/码头不得仅凭坐标接近自动合并。
47. 2026-07-31 已将正式运价源的真实精确 OD 驳船运价接入第二业务段：最新且唯一的精确 OD 报价优先于区域级驳船费率，费用仍为费率乘以订单数量，完整运输段时效来自独立时效表；存在精确 OD 但订单范围不适用或同日价格冲突时，不得回退区域费率掩盖问题。
48. 2026-07-31 已完成源数据能力和区域适配：驳船运价记录只证明该 OD 对应包装/品种下的端点作业能力，不自动改变客户、南港或中转港身份；35 个规范端点已全部匹配节点和时效区域，其中 32 个为维护映射、3 个为保留参考节点及距离的最近区域代理。
49. 2026-07-31 已实现客户近端的受控多式联运候选展开：保留按客户距离和真实汽运优先级形成的直接方案，同时为存在可完整构边的精确 OD 驳船证据的南港保留少量候选名额；每个南港按距客户直线距离依次检查中转港，直到形成最多 3 条完整“驳船+汽运”路径或候选耗尽。该数量是阶段 18 的搜索宽度策略，不是港口连通性规则，完整 OD 覆盖仍由离线审计验证。
50. 2026-07-31 新工作簿共选出 193 条驳船费率，最新价格冲突为 0；玉米范围 163 条、小麦范围 133 条均已完成离线构边验证。
51. 2026-07-31 已完成本轮代码审查收束：不同名称节点不得只凭坐标近邻合并；客户设施判定统一复用节点角色分类器；直接运输优先候选不得挤占全部多式联运名额；无效近端中转候选不得遮蔽更远但完整可用的驳船路径；“有精确 OD 报价”不得等同于“可构边”，必须同时通过订单范围、能力、连通性、费用和时效检查。
52. 2026-07-31 已将 `运费数据.xlsx` 的“运价表”工作表接为优先正式运价源：严格校验 9 列表头、工作表名称和维护日期，保留 Excel 行号追溯；只有工作簿不存在时才回退 `运价表.json`，工作簿存在但结构错误时失败关闭，不使用旧 JSON 掩盖问题。
53. 2026-07-31 新工作簿已通过“适用品种”拆分小麦与玉米/大豆费率，原 10 组同日同 OD 驳船报价冲突归零。对尚未进入节点能力主表的 `东莞粤储粮港务有限公司/东莞粤储粮` 和 `南宁港牛湾作业区`，已加入仅限明确名称、区域、功能角色和精确 OD 包装/品种证据的窄范围能力覆盖；不得据此推广到其他节点或其他业务范围。
54. 2026-07-31 南港身份筛选已改为“维护标签优先、名称规则补充”：优先读取 `节点信息维护0731.xlsx` 的节点类型、节点性质、码头属性和包装方式；若新版主表缺失则使用 `码头信息维护0729版.xlsx` 的港口码头性质。正式标签确认 `节点性质=港口码头` 后，名称不含“港/码头”或包含“公司”均不再单独导致排除；反之，租赁库、客户等节点不能仅凭码头属性或名称升级为南港。纯内河港、包装能力和明确业务排除仍继续生效。
55. 2026-07-31 标签优先改动最终独立全量测试为 `459 passed in 2.02s`；真实数据 smoke 与授权 Tencent 探针通过。真实 `北良港 -> 广西富丰集团有限公司` 案例中，`东莞新沙`、`广西铁山东岸码头有限公司` 已形成正式图边，`东莞市深粮物流有限公司` 按“租赁库”标签继续排除。统一 smoke 包装器因固定 pytest 临时目录被 Windows 锁定而失败，已用独立唯一 `basetemp` 的全量测试替代，没有删除锁定目录。
56. 2026-07-31 Web 已接入本地 `航线控制点_全点版.json`：当前文件包含 5 条走廊、298 个 GCJ-02 控制点。散船使用南北主走廊、福建—珠三角及珠三角—粤西—海南—广西网络，驳船使用闽江或西江网络；每个水运段的实际起终点分别接入同一可用网络中距离最近的控制点，再沿控制点子路径绘制。该几何只用于地图示意，不写入 `TransportEdge`，不影响费用、时效、候选、构图或双目标搜索；文件缺失时保留原明确示意回退，文件存在但结构或坐标系错误时显式失败。
57. 2026-08-06 已完成集装箱运输首轮数据契约：新增北港—南港 `集装箱船运价时效.csv` 的严格类型化接口、只读南港准入审计和虚构样表。首轮仅支持 `集装箱/箱`；北港—南港尚无已确认真实运价/航运总时效时只输出缺口、不构边，绝不复用散粮散船、散粮驳船、铁路或汽运数据。真实节点维护表审计识别 28 个“海港或海河一体 + 集装箱能力”候选，均因正式集装箱船数据源未接入而保持 `eligible_pending_rate_time`，不代表已上线集装箱路线。专项回归 `24 passed`；业务代码变更后的完整开发 smoke 为 `468 passed`，真实数据 smoke 通过，受限网络 Tencent 探针安全返回 `manual_review`。
58. 2026-08-20 启动铁路—集装箱基础算法验证：新增北站—南站铁路干线的类型化测试 Provider，费用组成含铁路执行运价、上/下站费和敞顶箱篷布费，时效按完整运输段字段表达；测试记录统一标为 `demo_placeholder`，不接入现有 full-flow/Web 或真实数据。东北至福建、广东、广西时效分别已确认 168、192、192 小时；铁路运费、站点费和专用线费用/时效未确认，继续阻断真实构边。
59. 2026-08-26 确认 `运输线路控制点_2026-08-26 完全补充版.csv` 为与散船航线控制点图同级的最新铁路 Web 展示基础源（679 个控制点、31 个线路实例）。该源仅用于北站—南站的线路示意、展示路径选择、控制点连通性审计和地图渲染；不进入费用、时效、候选、`TransportEdge`、正式搜索图或业务准入。线路实例键使用 `route_id + route_name`；原始名称保留。展示侧审计确认 11 个北站、50 个南站均可按名称命中该源；目标是让任意北站—南站 OD 尽可能沿已维护路线连通。若维护路线存在缺口，只能输出明确的展示补线待确认，不得伪装为真实铁路业务边。
60. 2026-08-31 完成铁路—集装箱南站至客户工厂三类末端方案的隔离骨架：直达拖车、客户专用线、第三方专用线中转。直达拖车使用确认的铁路末端专属元/箱距离规则（0—10km=450，10—20km=500，超过20km为 `500 + (X-20)*30*0.6`），不覆盖通用集装箱汽运 0.55 系数；客户专用线形成一条 `south_to_customer/delivery` 铁路边；第三方专用线显式形成南站至第三方的 `transfer` 铁路边和第三方至客户的 `delivery` 边。所有记录均须同时具备订单范围、费用、时效和来源；缺失或多条匹配只返回 `manual_review`，测试占位数据不得接入 CLI、Web 或真实图。
61. 2026-08-31 新增 `src.demos.rail_container_interactive_demo` 作为独立算法试算工具。北站—南站的运价、站点费、时效及专用线参数仍由人工输入并强制标为 `demo_placeholder`，不写入 `data_REAL`、不注册到 `leader.py`、不接入 Web、`leader_full_flow.py` 或正式图。末端方案选择“直达拖车/自动”时，工具复用本地标准节点优先、Tencent 地点检索回退、Tencent 普通驾车距离/时效和既有铁路末端拖车计费规则；该一段按实际来源独立追溯，Tencent 或坐标失败时返回 `manual_review`，不得补零。它用于验证北站—南站干线与三类末端方案的费用组成、时间累加、单位约束和边结构，不是项目成品入口。
62. 2026-08-31 新增 Web 独立页面 `rail_calculator.html` 与 `/api/rail-freight-calculator`：为业务人员复刻 `铁路测算模型部分测算数据/计算器.xlsx` 的铁路报价试算与费用分项审计。该计算器不读取或回写正式 OD 运价、不生成 `TransportEdge`、不参与候选或最短路。地方运费2已按业务确认由原表错误公式改为“折算基数 × (1 - 下浮率)”；原表固定 60 吨单价则保留为对照，同时展示按本次输入装载吨数计算的单吨价。
63. 2026-09-02 新增 `RailContainerPlanningService` 与 `rail_container_platform_demo`。脱敏平台快照将用户请求、节点、精确北站—南站铁路运价时效和南站直达拖车记录分离输入；应用服务从所有订单适用的精确南站 OD 构建 `north_to_south/trunk` 与 `south_to_customer/delivery` 边，只让完整候选进入带标准节点校验的 `nx.MultiDiGraph`，再独立执行 cost/time Dijkstra。缺少末端数据的南站只输出 `manual_review`，不以其他站代理。当前样表所有业务数值为 `demo_placeholder`，真实表/API Adapter 接入后只替换数据提供方，不替换 Provider、构图或搜索逻辑。
64. 2026-09-02 已接入第一批本地铁路测试工作簿的只读 Adapter：`测试路线/敞顶箱-8.28查询` 已按用户确认从元/组（2 箱/组）转换为铁路干线元/箱，适用于内贸玉米/小麦敞顶箱；北/南站装卸费按韶关市站点 195 元/箱、其余站点 136.5 元/箱，敞顶箱篷布 250 元/箱，堆存和延箱当前不考虑。真实加载验证 101 条确认价格中 66 条普通站—站记录已成为 `real_data` 干线记录；34 条“站转专用线”复合终点保留给后续专用线末端方案，1 条缺北站不构边。工作簿不复制入仓库，未接入 full-flow/Web 默认入口。
65. 2026-09-08 已建立铁路集装箱本地数据接入和统一应用入口：通用 Excel 表头映射与本地数据源清单将站点主数据、干线费率、站点费、时效和三类末端记录分开读取、审计和准入；已准入记录自动生成铁路干线与完整末端平行边。Web 路线页允许用户选择“散粮/铁路/混合”方案范围：散粮约束为 `散粮/吨`，铁路约束为 `集装箱/箱`，混合仍按包装和单位筛选兼容 Provider，不做吨/箱/柜转换。铁路正式本地源必须由 `RAIL_CONTAINER_SOURCE_MANIFEST` 显式配置；页面联调可另行显式指定 `RAIL_CONTAINER_DEMO_FIXTURE`，其结果保持 `demo_placeholder`，不得冒充真实报价。当前集装箱船正式数据仍未接入，故集装箱订单目前只会比较已准入铁路方案；未来新增集装箱船 Provider 后应在同一集装箱应用服务中并行比较，而非新增前端分支。

阶段 18 的详细执行计划保存在本地内部文档中；公开 GitHub 仅保留脱敏 Issue #1，不上传内部业务计划正文。

## 阶段 18 输入清单

- 北港至南港散船费用、运输时间和来源维护规则；
- `散船运价表.xlsx` 的价格单位为元/吨，费用公式为报价乘以订单吨数；只表示北港至南港散船粮食运价，不含港口作业费，税务问题当前不考虑；
- 散船运价表适用于本项目全部北港，其他北港与北良港具有一致适用性；
- 散船运价表中不属于广东、广西、福建、海南的目的地列不读取、不修改，也不进入本阶段正式候选；
- 南港作业费第一版采用汇总费用类型“码头作业费”，业务上先只考虑“入库费”；入库费为单位费率，散粮使用 `元/吨`，集装箱预留 `元/箱`，后续如有变动再通过 Provider/CSV 细分卸船费、堆存费、短倒费等；
- 订单和作业费数据需保留 `tradeType`/`trade_type`：当前主要研究 `内贸`，但接口需支持 `外贸`；同一码头可能按品种、贸易类型和包装方式维护不同费率；
- 客户是否有自有码头、对应码头节点和字段来源；
- AdditionalFee 的运输段归属、叠加条件和重复费用判定；
- 当前批次缺节点治理结果：运价表 295 个不同地点名称已全部匹配，312 条已计费候选的缺节点 ID 数为 0；
- 已具备去重清单和 Tencent 候选坐标复核工具；本批坐标回写及别名确认已完成，后续新增地点仍须沿用人工审查；
- 腾讯地点与路线结果的抽检、缓存、确认和回写协议；
- 正式运输时效来源，替换 `REAL_DATA_DEMO_MANUAL_TIME_HOURS`；
- 船运时效治理：当前模型将业务提供的“航行时效”直接视为对应航运段总时间，不拆分等待、装船、实际航行、卸船等组成；散船干线已有区域时效，驳船已建立独立区域时效表接口，未配置的航运段仍须提示且不得补零；铁路时间仍待后续数据源；
- 人工指定南港模式：若用户显式输入南港，则北港—南港作为固定干线段直接计算费用/时效，不参与多南港图搜索；后续搜索问题收缩为南港—客户，并将固定干线段加回总结果展示。
- 港口能力表接口：以标准 `node_id` 为主键，维护基础设施类型、是否可接散粮散船、是否可走内河驳船、支持包装方式、支持品种、所属内河区域、来源和维护日期；缺失能力不生成对应运输边。
- 区域映射表接口：维护节点/城市/港区到散船费率目的组、完整航运段时效分区、内河区域的映射，并保留来源；无法唯一映射时进入人工复核，不按名称猜测。
- 南港码头作业费表接口：`chargeable` 维护正数单价，`not_applicable` 仅用于经确认无需作业费的客户自有码头且必须维护 0 与原因；散粮接受 `元/吨`、集装箱预留 `元/箱`，订单维度必须精确匹配，缺失仍不视为 0；字段模板见 `docs/data_templates/南港码头作业费.example.csv`。
- W5 显式地域代理接口：`区域映射表.csv` 以标准港口 `node_id` 维护 `operation_fee_region_code`、映射依据、规则编号/版本和确认状态；`南港码头作业费.csv` 显式标记区域参考码头。唯一已确认映射与唯一适用参考费率同时存在时优先生成 `regional_proxy`；未命中后才进入下一优先级的最近区域代理。
- W5 最近区域代理：领导展示阶段在精确费率和显式 `operation_fee_region_code` 均未命中时，可使用区域映射或已确认散船分区解析同一业务区域，并选择该区域内地理距离最近的适用正式费率；必须完整保留参考港和距离，区域不唯一或坐标缺失时仍人工复核。
- `部分码头标签.json` 默认只作为 W5 候选规则源进行只读审计；只有用户明确批准并显式执行 `--apply-approved-w5` 时才生成正式表，且拒绝覆盖已有正式表。
- 内河航运航费/航时表接口：航费与时效允许分别维护；时效表维护起终区域、完整航运段时间、方向、规则编号/版本和来源，模型不拆分时间组成；正式航费或能力缺失时仍不生成真实边，Demo 只能生成明确 `demo_placeholder`。
- 福建闽江驳船已确认输入：当前只维护 `南平港↔军航码头`，往返完整运输段 15 小时、散粮 50 元/吨；两端当前共同支持的正式品种为玉米和小麦。南平港同时支持集装箱和铁路，但相应集装箱/铁路费用及时效仍需独立数据接口。
- 北港散船南港准入要求：节点维护表中的节点性质是港口身份主证据，码头属性用于区分海港/内河港，包装方式用于订单能力校验；维护标签缺失时名称规则才作为补充。明确海港/海河双用标识可以准入；对于水域角色尚未分类的标准港口/码头，适用当前品种的散粮始发运价也可作为业务准入证据。已确认纯内河港、铁路站、客户设施和显式散粮能力排除始终优先；仅集装箱始发运价不能证明散粮南港准入。
- 本地展示页接口：输入北港、客户工厂、可选南港、区域、品种、包装、重量、计费单位和贸易类型；输出双目标路线、费用分项、提示/错误和地图分段几何。汽运使用腾讯真实道路折线；散船和当前驳船仍为明确示意。数据库、鉴权、部署和权威航道几何不在当前阶段。
- 2026-07-24 只读数据夯实审计：运价地点未注册为 0；AdditionalFee 仍有 2 个站点类节点未注册，列入后续铁路/集装箱节点治理；散船运价表 `秀屿` 目的标签已按用户确认纳入福建映射；正式 W3/W5 表当前尚未填充真实记录。

## 停止并确认条件

出现以下情况时停止实现并提交用户审核：

- 公式、区间、单位、方向、价格类型或费用归属存在歧义；
- 作业费需要跨品种、贸易类型、包装方式或 `吨/箱/柜` 做推断、合并或换算；
- 需要改变已确认的陌生散粮或集装箱汽运公式；
- 需要在 `吨`、`箱`、`柜` 间换算；
- 需要扩大领导 Demo 的占位数据范围；
- 需要在未披露 `demo_placeholder` 能力来源的情况下生成驳船边，或把能力占位描述为正式港口能力；
- 需要在福建闽江或已确认珠三角—西江沿线范围以外生成内河驳船边；
- 需要更改多候选默认 top1、置信等级或人工复核口径；
- 需要提交真实业务数据、腾讯 Key、本地环境文件或用户办公材料；
- 需求与测试、当前代码或 `docs/DECISIONS.md` 冲突。

当前仍需要用户确认后才能继续扩展正式路线覆盖的事项：

- `东莞深粮` 出发的原 10 组精确 OD 驳船报价已由新版 `运费数据.xlsx` 按小麦与玉米/大豆拆分，不再属于待确认冲突；后续若出现同业务范围、同一最新日期的不同报价，仍保持 `manual_review` 且不得由区域费率覆盖；
- 2026-07-29 已确认并收束 `洋浦港`、`军航码头` 及四个内河码头的北港散船准入角色，不再列为待确认项；
- 南平港的散粮/集装箱、玉米/小麦、驳船/铁路能力及禁止作为海运散船南港终点均已确认，不再列为待确认项；
- 当前没有海港/海河双用标识、也没有适用散粮始发运价证据的港口仍需分批确认水域角色；仅集装箱始发端需在未来独立确认是否具有中转港功能角色；
- 南平港的集装箱驳船和铁路运输尚缺各自费用及时效数据；不得复用散粮 50 元/吨规则；
- 福州马尾港当前只有集装箱玉米/小麦能力，但已退出当前维护的闽江航线；后续如重新纳入需单独确认线路和运费/时效；
- 福州松下码头的散粮作业能力尚未确认，且已确认其不属于当前闽江驳船走廊；
- 当前客户工厂统一按无自有码头处理；未来若接入客户自有码头，必须由正式客户—码头关系数据显式提供；
