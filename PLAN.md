# PLAN.md

Updated: 2026-07-21

本文件只记录当前真实工程计划。完成状态以当前代码、测试和 `docs/PROJECT_STATE.md` 为准；领导汇报、每日计划和历史报告不能替代工程事实。

## 开发主线

```text
本地真实数据
-> 数据审计与类型化加载
-> 节点、坐标、订单和单位校验
-> 运价选择、费用和时间 Provider
-> CustomerProfile 路线分支
-> TransportEdge
-> MultiDiGraph
-> cost / time 独立搜索
-> RouteResult 可解释输出
-> 全流程 Demo
-> 真实业务数据替换占位
-> 展示收束与后续生产化评估
```

## 里程碑状态

| 阶段 | 里程碑 | 状态 | 验收依据或剩余边界 |
|---|---|---|---|
| 0 | 长期项目记忆和交接体系 | 已完成 | `AGENTS.md`、`PLAN.md`、`PROJECT_STATE.md`、`ARCHITECTURE.md`、`DECISIONS.md`、`CHANGELOG.md` 已建立 |
| 1 | JSON/JSONL 数据审计和脱敏输出 | 已完成（第一版） | 支持质量统计；详细输出只保留在本地 `output/` |
| 2 | 真实数据类型化加载 | 已完成（第一版） | 通过 `DATA_DIR` 加载运价、节点和 AdditionalFee，异常显式返回 |
| 3 | NodeRegistry 和坐标解析 | 已完成（基础版） | 本地优先、腾讯兜底；缓存、抽检和正式回写未完成 |
| 4 | RouteRequest、单位和包装校验 | 已完成（第一版） | `吨/箱/柜` 与价格单位精确匹配，不自动互换 |
| 5 | FreightRate、CostRuleEngine 和最新运价 | 已完成（第一版） | 熟悉路线、散船模式、陌生散粮和集装箱汽运规则均有测试与追溯 |
| 6 | ShippingTimeProvider | 已完成（第一版） | 人工 Provider 可用，JSON/数据库/API Provider 仅保留接口 |
| 7 | CustomerProfile 和路线分支 | 已完成（第一版） | 自有码头与候选中转港分支已建模；真实客户字段未接入 |
| 8 | TransportEdge 和真实候选桥接 | 已完成（保守第一版） | 节点、费用、时间和来源齐全才形成可搜索边 |
| 9 | `nx.MultiDiGraph` 正式图 | 已完成（第一版） | 保留平行边，以 edge ID 为 key，排除项可追溯 |
| 10 | cost/time 双目标搜索 | 已完成（第一版） | NetworkX Dijkstra 独立搜索成本和时间，不合成权重 |
| 11 | RouteResult 和解释输出 | 已完成（第一版） | 返回节点、edge key、分段费用/时间/来源和显式异常状态 |
| 12 | 代码分层和旧原型剥离 | 已完成（第一版） | `data/domain/geo/routing/demos/dev` 分层，旧 CSV/DiGraph 主链已移除 |
| 13 | 本地运行环境表和开发者 smoke | 已完成（第一版） | 本地 CSV 注入环境变量；配置模板可提交，真实配置被忽略 |
| 14 | 腾讯地点多候选策略 | 已完成（原型口径） | 多候选默认 top1，不阻塞；保留 top 5 和置信等级供抽检 |
| 15 | 真实数据候选进入正式搜索链 | 已完成（保守第一版） | 首批节点补录后，本地演示人工时效下 152 条边可入图；该时效不代表真实业务值 |
| 16 | 第一版全流程领导 Demo | 已完成（基础版） | A/B 输入到费用最低、时间最短路线已贯通；占位范围显式受限 |
| 17 | PyCharm 真实 A/B 预演与展示收束 | 进行中 | 需使用本地 Tencent Key 完成全流程预演、核对中文输出和演示输入 |
| 18 | 用正式业务数据替换演示占位 | 未开始 | 依赖北港至南港费用/时效、客户自有码头字段、AdditionalFee 归属和缺失节点治理 |
| 19 | 生产化范围评估 | 未开始 | 数据库/API/前端、缓存、权限、监控和性能均未立项，不得视为已有能力 |

## 当前里程碑

阶段 16 标志着项目最基础的端到端模型已经完成。当前只推进阶段 17，不继续扩大功能范围：

1. 在用户 PyCharm 环境运行 `python -B -m src.demos.leader full-flow`；
2. 选择用于展示的北港 A 和客户工厂 B，核对 Tencent 地点、道路距离和路线输出；
3. 确认费用最低与时间最短结果、来源标签和最小伪造声明可被现场解释；
4. 只修复阻塞展示的问题，避免在展示前引入新的业务规则或大规模重构。

## 阶段 18 输入清单

- 北港至南港散船费用、运输时间和来源维护规则；
- 客户是否有自有码头、对应码头节点和字段来源；
- AdditionalFee 的运输段归属、叠加条件和重复费用判定；
- 162 条缺节点 ID 候选的后续节点补录或别名治理结果；
- 已具备去重清单和 Tencent 候选坐标复核工具；实际坐标回写及别名确认仍须人工审查；
- 腾讯地点与路线结果的抽检、缓存、确认和回写协议；
- 正式运输时效来源，替换 `REAL_DATA_DEMO_MANUAL_TIME_HOURS`。

## 停止并确认条件

出现以下情况时停止实现并提交用户审核：

- 公式、区间、单位、方向、价格类型或费用归属存在歧义；
- 需要改变已确认的陌生散粮或集装箱汽运公式；
- 需要在 `吨`、`箱`、`柜` 间换算；
- 需要扩大领导 Demo 的占位数据范围；
- 需要更改多候选默认 top1、置信等级或人工复核口径；
- 需要提交真实业务数据、腾讯 Key、本地环境文件或用户办公材料；
- 需求与测试、当前代码或 `docs/DECISIONS.md` 冲突。
